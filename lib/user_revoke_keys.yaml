---
#
# revoke user keys from all hosts
#   
- hosts: all

  tasks:
  - include_vars: "{{ pmaker_home }}/etc/config.yaml"

  # - debug:
  #     var: username
      
  - include: user_revoke_key.yaml
    with_items: 
      - "{{ username }}"
