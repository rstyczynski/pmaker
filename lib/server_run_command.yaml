- hosts: all
  gather_facts: no
  tasks:
     - name: Run the command
       command:
          argv: 
            - "{{ command }}"
            - " {{ params }}"
       register: out
     - debug: msg="{{ command }} {{ params }}"
     - debug: var=out.stdout_lines
