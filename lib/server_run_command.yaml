- hosts: all
  gather_facts: no
  tasks:
     - name: Run the command
       command: "{{ command }} {{ params }}"
       register: out
     - debug: "{{ command }} {{ params }}"
     - debug: var=out.stdout_lines