---
- name: Create pmaker state directory
  file:
    path: "{{ pmaker_home }}/state/{{ user_group }}/{{ server_group }}/pmaker/.ssh"
    state: directory
    owner: "{{ item }}"
    mode: u+r,g-w+x,o+x
    recurse: yes

